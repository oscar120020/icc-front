import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  containerParams: {
    display: 'flex', 
    flexDirection: 'row', 
    justifyContent: 'space-evenly'
  },
  text: {
    fontSize: '18px',
  }
});
