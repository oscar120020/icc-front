import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  containerParams: {
    flexDirection: 'row', 
    justifyContent: 'space-evenly'
  },
  text: {
    fontSize: '18px',
  }
});
